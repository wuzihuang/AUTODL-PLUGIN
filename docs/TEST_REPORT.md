# AutoDL Skill 与 MCP 测试报告

测试日期：2026-07-20（Asia/Shanghai）

## 交付范围

- AutoDL Skill：操作规则、安全边界、API 参考、运维指南、官方文档索引和刷新/校验脚本。
- AutoDL MCP：26 个 stdio 工具。
  - 三份核心开发者 API 文档中的 24 个操作全部覆盖。
  - 帮助站其他页面中单独记录的微信通知与容器本地性能指标也已覆盖。
- 使用的运行时：Node.js 20；`@modelcontextprotocol/sdk` 1.29.0；Zod 4.4.3。

## 文档覆盖测试

| 项目 | 结果 |
|---|---|
| 官方搜索索引条目 | 463 |
| 归并后的实际页面 | 103 |
| 无缓存逐页访问 | 103/103 HTTP 成功 |
| 页面审计字段 | URL、标题、章节、正文字符数、HTML 字节数、HTML SHA-256、索引正文 SHA-256 |
| API 深度整理 | 通用 API、容器实例 Pro API、弹性部署 API、微信消息、容器指标 |

文档正文没有整站复制进 Skill。Skill 保存逐页路由索引、哈希清单和经整理的操作规则；需要最新信息时由刷新脚本重新访问官方页面。

## MCP 契约测试

通过本地 mock AutoDL 服务和真实 MCP stdio 握手执行：

- 26/26 工具成功注册。
- 26/26 工具均完成一次完整调用并命中预期 HTTP 方法和路径。
- 验证 Pro 状态/详情接口的 `GET + JSON body` 行为。
- 验证弹性部署同一路径的不同方法，例如黑名单 `POST/GET` 和部署 `POST/DELETE`。
- 验证 DDP 时长包使用 query 参数。
- 验证未设置 `confirm=true` 的变更请求只返回预览，网络请求数保持为 0。
- 验证 AutoDL 业务错误转换为 MCP 错误，并保留 `code` 与 `request_id`。
- 验证开发者 Token 不进入工具输出。
- 验证容器本地指标请求不携带开发者 Token。
- 验证实例详情默认遮盖 `root_password` 和 `jupyter_token`。

测试命令 `npm test`：2 个测试套件通过，0 失败。

## AutoDL 正式环境只读测试

使用用户提供的测试 Token，仅执行不会改变账户状态的请求：

| 检查 | 结果 |
|---|---|
| 获取账户余额 | PASS，HTTP 200 |
| 获取 Pro 实例列表 | PASS，HTTP 200 |
| 获取 Pro 私有镜像列表 | PASS，HTTP 200 |
| 获取弹性部署列表 | 权限跳过：账号返回“无当前资源访问权限” |
| 获取弹性部署黑名单 | 权限跳过：账号返回“无当前资源访问权限” |
| Pro 状态/详情 | 条件跳过：账号当前没有 Pro 实例 |

没有在正式环境执行创建、开关机、释放、保存镜像、部署伸缩/停止/删除、NFS 切换、黑名单设置或微信消息发送。

## 打包与安全检查

- `npm pack --dry-run` 通过，MCP 包可生成。
- Skill frontmatter 校验通过。
- Skill API 参考覆盖 26/26 MCP 工具。
- Skill 与 MCP 交付目录未发现 JWT 形式的 Token。
- 真实测试 Token 未写入源码、配置示例、文档、测试报告或压缩包。
- Skill 已安装到本机 `~/.codex/skills/autodl/`。
- MCP 配置示例已生成，但没有把测试 Token 持久化到 Codex 配置；用户应换用长期开发者 Token 后再启用。

压缩包 SHA-256：

- `autodl-mcp-server-1.0.0.tgz`: `dc2ab0f00adf12114735c191417bdf3a040ba1dd31a5713f4cafdf2fe4ae84b5`
- `autodl-skill.zip`: `35bf1159476cddba5f37b595cf3142a255d345acdd3abe688841199a65c919c9`

## 尚未进行的真实验证

以下项目需要企业权限、现存测试资源，或会产生费用/改变数据，因此只做了完整 mock 契约验证：

- 弹性部署全部正式环境调用。
- Pro 实例状态与详情的正式环境调用。
- 所有创建、开关机、释放、保存镜像、伸缩、停止、删除、存储切换和消息发送操作。

这不表示对应工具不可用；它表示当前测试账号或安全边界不允许进行无风险的正式环境验证。若要做端到端写操作验收，应准备专门的企业测试账号、零重要数据的测试实例和明确的费用上限。
